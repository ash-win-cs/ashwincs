export type skillsModel = {
    id: number;
    skillName: string;
    score: number;
    classification: string;
};

export type socialHandles = {
    id: number;
    socialIconName: string;
    socialUrl: string;
};